import { useLocation } from "react-router-dom";
import Header from "../../components/Header";
import "./Login.css";

const Login=()=>{
    
    return(
        <body>
            <Header/>
        </body>
    );
}

export default Login;
